# Optional: Increases GitHub API rate limit from 60/hr to 5000/hr
# Used by scripts/fetch-github-data.mts for local data fetching
# NOT required for the site to function — client-side uses unauthenticated requests
GITHUB_TOKEN=your_github_pat_here

