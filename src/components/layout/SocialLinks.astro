---
/**
 * SocialLinks — Shared social media link icons.
 *
 * Used in both Sidebar and MobileNav to eliminate SVG path duplication.
 */
import { SOCIAL_ICONS } from '@config';
---

<div class="border-border flex items-center gap-3 border-t pt-4">
  {
    SOCIAL_ICONS.map(({ href, label, path }) => (
      <a
        href={href}
        target="_blank"
        rel="noopener noreferrer"
        aria-label={label}
        class="text-text-muted hover:bg-bg-surface hover:text-accent flex h-9 w-9 items-center justify-center rounded-lg transition-colors duration-100"
      >
        <svg class="h-5 w-5" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
          <path d={path} />
        </svg>
      </a>
    ))
  }
</div>
