---
import RepoCard from '@components/ui/RepoCard.astro';
import { loadRepos } from '@lib/data/loader';

const repos = (await loadRepos()) ?? [];
---

<section id="projects" aria-label="Active projects">
  <h2 class="mb-2 text-2xl font-bold tracking-tight sm:text-3xl">Active Projects</h2>
  <p class="text-text-secondary mb-6">Open source projects I maintain and contribute to</p>

  <div class="grid grid-cols-1 gap-4 lg:grid-cols-2">
    {repos.map((repo) => <RepoCard repo={repo} />)}
  </div>
</section>
